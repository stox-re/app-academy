// N.B.: we'll skip turbolinks because it prevents full page reloads,
// which can lead to bugs in your JavaScript frontend
// Create a new rails project using --database=postgresql and --skip-turbolinks